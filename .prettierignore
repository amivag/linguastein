dist/
coverage/
node_modules/
dev-dist/
docs/spec/
*.jsonl

# Generated by scripts/build-dataset.ts. Prettier and the generator disagree on
# how to wrap short arrays, and CI checks both `prettier --check` and
# `git diff --exit-code public/packs` — so formatting this directory makes one of
# the two fail whichever way the files are committed.
public/packs/
